# VSCodium Sync Settings

This repository acts as the remote storage backend for the VSCodium [Sync Settings](https://open-vsx.org/extension/zokugun/sync-settings) extension. It automatically tracks `settings.json`, `keybindings.json`, and the installed extension manifest.
